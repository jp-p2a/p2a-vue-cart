const BASE_ENDPOINT = `https://${window.location.hostname}`;

const PRODUCTS = 'products';

const BILLING_INFO = 'billingInfo';

const ORDER = 'order';

export {
  BASE_ENDPOINT,
    PRODUCTS,
    BILLING_INFO,
    ORDER
};
